<Page x:ConnectionId='1'
    x:Class="Sassy_Saloons.Pages.salComment"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:data="using:Sassy_Saloons.Models"
                        
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="0.15*"/>
            <RowDefinition Height="0.15*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Button x:ConnectionId='2' x:Name="moreButton" Style="{StaticResource buttonStyle}"
                Grid.Row="2" FontSize="30"
                Content=""                         />

        <Button x:ConnectionId='3' x:Name="commentButton" Style="{StaticResource buttonStyle}"
                Grid.Row="2" Grid.Column="1"
                Content="" FontSize="30"
                Background="Teal" Foreground="White"/>

        <ListView x:ConnectionId='4' x:Name="commentsList"
                  ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                  ScrollViewer.VerticalScrollBarVisibility="Hidden"
                                                 >
           
            <ListView.ItemTemplate>

                <DataTemplate                           >
                    <StackPanel x:ConnectionId='7' Margin="0,10,10,0">
                        <TextBlock x:ConnectionId='8' x:Name="usernameBlock" 
                               VerticalAlignment="Center"
                               FontSize="23"                             
                               FontWeight="Bold" />

                        <TextBlock x:ConnectionId='9' x:Name="commentBlock"
                                   VerticalAlignment="Center"
                                   FontSize="20"                        />
                    </StackPanel>

                </DataTemplate>
            </ListView.ItemTemplate>
            
        </ListView>

        <ListView x:ConnectionId='5' x:Name="starList" Grid.Column="1"
                  ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                  ScrollViewer.VerticalScrollBarVisibility="Hidden"
                                                 >
            <ListView.ItemTemplate>
                <DataTemplate                           >
                    <ProgressBar x:ConnectionId='6' x:Name="starsHere"
                                 Width="60" Height="10" 
                                                      
                                 Maximum="5"/>

                </DataTemplate>
            </ListView.ItemTemplate>

        </ListView>

    </Grid>
</Page>

